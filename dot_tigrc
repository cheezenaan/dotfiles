# メイン画面でコミットIDを表示する
set main-view = id date author commit-title:graph=yes,refs=yes

# I キーで "git rebase -i" を実行できるようにする
bind main    I !git rebase -i %(commit)
bind diff    I !git rebase -i %(commit) 
